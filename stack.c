#include "monty.h"

/**
 * print_stack - prints value at the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
*/
void print_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}

